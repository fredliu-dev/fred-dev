{% if image != blank %}
  <img
    src="{{ image| image_url }}"
    width="40"
    height="40"
    class="lazyload"
    loading="lazy"
  >
{% else %}
  {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
{% endif %}
